[project]
name = "sentry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "async>=0.6.2",
    "livekit>=1.1.10",
    "pipecat>=0.3.0",
]
